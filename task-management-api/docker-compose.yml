# Docker Compose for PostgreSQL (optional)
# Use this when you want to switch from H2 to PostgreSQL:
#   1. docker-compose up -d
#   2. mvn spring-boot:run -Dspring-boot.run.profiles=postgres

version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: taskdb-postgres
    environment:
      POSTGRES_DB: taskdb
      POSTGRES_USER: taskuser
      POSTGRES_PASSWORD: taskpass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taskuser -d taskdb"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
